//<b>Destructuring</b> is a JavaScript expression that makes it possible to unpack values from arrays, or properties from objects, into distinct variables.

const [a, b] = [1, 2];
const { x, y } = { x: 10, y: 20 };